using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class MiniMapController : MonoBehaviour {

	public GameObject target;

	// Use this for initialization
	void Start () {
		
	}
	
	// Update is called once per frame
	void FixedUpdate () {
//		Vector3 targetLoc = target.transform.position;
//		this.transform.position = new Vector3 (targetLoc.x, 4000 , targetLoc.z);
//
//
//		Vector3 targetRot = target.transform.rotation.eulerAngles;
//		targetRot.y = 0.0f;
//		//targetRot.z = 0.0f;
//		//targetRot.x = 0.0f;
//		this.transform.rotation = Quaternion.Euler( target.transform.forward);

//		Vector3 targetPosition = target.transform.position;
//		targetPosition.y = 20000;
//
//		this.transform.position = targetPosition;
////
//		Vector3 oldRotation = target.transform.rotation.eulerAngles;
//
//		//oldRotation.y = 0.0f;
//		oldRotation.z = 0.0f;
//		oldRotation.x = 90.0f;
//		oldRotation.y += 180.0f;//target.transform.rotation.y;
//		this.transform.rotation = Quaternion.Euler(oldRotation);
	}
}
